# ============================================================
# JUMP0X1 Configuration
# ============================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Trading Mode: paper (simulation) or real (actual trading)
TRADING_MODE=paper

# EXECUTION GATE: Must be 'true' to place real orders (safety lock)
# Requires BOTH: TRADING_MODE=real AND EXECUTION_ENABLED=true
EXECUTION_ENABLED=false

# Max live trades per run (0 = unlimited, 1 = training wheels)
MAX_LIVE_TRADES_PER_RUN=1

# ============================================================
# POLYMARKET CREDENTIALS (required for REAL mode)
# ============================================================

# Your wallet private key - NEVER share this!
# Get from MetaMask: Settings > Security > Reveal Private Key
PM_PRIVATE_KEY=0x_your_private_key_here

# Your wallet address (0x...) - Polymarket proxy wallet
PM_WALLET_ADDRESS=0x_your_wallet_address_here

# Funder address (usually same as wallet address for proxy)
PM_FUNDER_ADDRESS=0x_your_funder_address_here

# Signature type (0 = EOA, 1 = Poly proxy, 2 = Gnosis)
PM_SIGNATURE_TYPE=2

# ============================================================
# POLYGON RPC (optional - uses public endpoint by default)
# ============================================================

PM_POLYGON_RPC_URL=https://polygon-rpc.com
PM_USDC_CONTRACT=0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359

# ============================================================
# STRATEGY SETTINGS
# ============================================================

# Trade size ($5 minimum for Polymarket)
PM_CASH_PER_TRADE=5.00

# Max position size per session
PM_MAX_POSITION=8.00

# RULEV3+ thresholds
PM_EDGE_THRESHOLD=0.64
PM_SAFETY_CAP=0.72

# Zone mode: T3-only (CORE only) or T3+T5 (CORE + RECOVERY)
PM_ZONE_MODE=T3-only

# ============================================================
# TELEGRAM NOTIFICATIONS (optional)
# ============================================================
#
# Notifications sent:
# - Every trade entry (direction, price, cost, zone)
# - Every trade settlement (WIN/LOSS, PnL, cumulative)
# - Every 10 sessions (summary: trades, W/L, PnL)
# - Daily summary at midnight (full day stats)
#

# Enable/disable Telegram notifications (1 = on, 0 = off)
TELEGRAM_ENABLED=0

# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Chat IDs - comma-separated for multiple (personal + group)
# Personal: send /start to @userinfobot to get it
# Group: add bot to group, send message, check getUpdates for negative ID
# Example: 123456789,-100987654321
TELEGRAM_CHAT_ID=
